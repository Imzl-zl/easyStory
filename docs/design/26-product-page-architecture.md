# 产品页面架构

| 字段 | 内容 |
|---|---|
| 文档类型 | 页面规划 / 功能定义 |
| 文档状态 | 建议方案 |
| 创建时间 | 2026-04-29 |
| 更新时间 | 2026-04-30 |

---

## 1. 页面总览

easyStory 包含以下页面：

| 序号 | 页面 | 路由族 | 功能定位 |
|---:|---|---|---|
| 1 | 认证 | `/auth/login`、`/auth/register` | 登录 / 注册 |
| 2 | 书架 | `/workspace/lobby` | 项目管理 + 公共配置 |
| 3 | 创作工作室 | `/workspace/project/[id]/studio` | 写作主场 + AI 对话 |
| 4 | 工作流引擎 | `/workspace/project/[id]/engine` | 自动化批量执行 + 工作流配置 |
| 5 | 分析实验室 | `/workspace/project/[id]/lab` | 研究外部文本 + 风格分析 |

---

## 2. 认证页

### 功能
- 登录
- 注册

---

## 3. 书架页

### 功能

**项目管理**
- 项目列表（卡片网格），每个卡片显示：项目名、字数、上次编辑时间、工作流运行状态
- 新建项目：点击即创建，直接进入工作室，无中间孵化页
- 回收站

**公共配置**（侧边面板）
- Skills
- Agents
- MCP
- 模型连接与凭证
- 账户信息

---

## 4. 创作工作室

### 功能

**文档树**（左侧栏）
- 按类别组织的项目文档：项目说明、设定、数据层、大纲（总大纲、开篇设计）、正文（章节列表）、时间轴、附录、校验
- 大纲、开篇设计、章节为数据库真值源，不可重命名/删除
- 自定义文件/文件夹支持重命名、删除
- 右键菜单：打开编辑、导出、查看版本
- stale 章节标记、生成中章节加载动画

**编辑器**（中间区域）
- Markdown 编辑器（章节正文、大纲、开篇、设定）
- JSON 编辑器（数据层）
- 侧边栏可折叠，折叠后编辑器自动撑满
- 保存状态显示、字数统计

**AI 共创助手**（右侧栏，可折叠）
- 上下文感知对话（当前打开文档自动带入）
- AI 可读文档、写文档、搜索、生成大纲
- 返回内容可一键应用、追加或创建新文档
- Skill / 模型切换
- 写入权限控制

**质量可见性**（分阶段落地）
- 第 1 层：文档树标记（stale / 审核 issue 颜色标记）
- 第 2 层：审校面板（issue 列表、冲突详情、批量操作）
- 第 3 层：AI 聊天解释与修复

**其他**
- 导出：文档树右键或工具栏触发，选择范围/格式/版本后下载

---

## 5. 工作流引擎

### 功能

**执行管理**
- 工作流启动、暂停、取消
- 执行历史列表
- 节点时间线（含状态、耗时、token 消耗）

**节点详情**
- 输入/输出查看
- 执行日志
- Prompt 回放
- 审核结果查看

**实时事件流**
- SSE 推送执行进度

**工作流配置**
- 工作流模板管理（创建、编辑、删除）
- 模板仅在引擎页内管理，不出现在书架或项目创建流程中

**与工作室联动**
- 引擎生成的章节自动出现在工作室文档树
- 引擎执行中可切回工作室继续写作

---

## 6. 分析实验室

### 功能

**文本分析**
- 上传外部参考文本（小说等）
- 选择分析类型：风格分析、人物分析、结构分析等

**分析报告**
- 展示分析结果

**Skill 生成**
- 从分析结果提炼可复用 Skill
- 生成的 Skill 回到工作室可用

---

## 7. 设置

设置以侧边面板形式存在于书架和工作室中，不跳页。

**个人设置**（书架页入口）
- 账户信息
- 模型连接
- AI 偏好
- 个人规则
- Skills / Agents / Hooks / MCP

**项目设置**（工作室入口）
- 基础信息
- 项目规则
- AI 偏好
- Skills / MCP
- 凭证
- 危险操作

---

## 8. 功能覆盖矩阵

| 后端模块 | 承载位置 |
|---|---|
| `user` | 认证页 + 设置面板 |
| `project` | 书架页（列表/创建/删除/回收站）+ 工作室（文档树） |
| `content` | 工作室文档树 + 编辑器 |
| `workflow` | 工作流引擎页 |
| `review` | 引擎执行审核节点 + 工作室审校面板 |
| `context` | 工作室数据层 |
| `assistant` | 工作室 AI 共创助手 |
| `analysis` | 分析实验室 |
| `credential` | 设置面板 |
| `billing` | 引擎节点 token 消耗 |
| `export` | 工作室导出对话框 + 引擎导出节点 |
| `observability` | 引擎页日志/Prompt 回放 + 设置面板审计 |
| `config_registry` | 管理员独立入口（不暴露给普通用户） |

---

## 9. 空状态提示

| 位置 | 空状态引导 |
|---|---|
| 书架页（无项目） | 引导新建，提供输入框直接开始 |
| 工作室（无章节） | 引导在编辑器写第一章或在 AI 聊天对话 |
| 引擎页（无执行） | 引导选择或创建工作流 |
| 实验室（无分析） | 引导上传参考文本 |
| 文档树数据层（空） | 提示 AI 可从正文自动提取 |

---

---

## 10. 设计决策记录

| 决定 | 结论 | 理由 |
|---|---|---|
| 不做独立孵化页 | 砍 | 新建项目直接进工作室，AI 聊天本身就在工作室里，不需要中间页 |
| 不做项目创建模板 | 砍 | 写小说不是搭脚手架，"修仙模板""悬疑模板"对创作者无实际帮助；工作流模板归引擎页管理 |
| 工作流模板放在引擎页 | 保留在引擎 | 模板的受众是工作流配置者，不是写作者，不应出现在书架或创建流程中 |
| 不设搜索/筛选项目 | 砍 | 写作者不会同时有几十个项目 |
| 项目卡片不显题材/状态 | 砍 | 需要用户手动维护的元数据是负担，保留项目名、字数、上次编辑时间即可 |
| 文档树保留时间轴、附录 | 保留 | 时间轴即小说时间线，附录有实际用途 |
| 文档树保留校验目录 | 暂留 | 功能尚不明确，后续再定 |
| 文档树不做导出记录 | 砍 | 导出就是下载文件，不需要在树里留记录 |
| 编辑器不做四视图模式 | 砍 | 侧边栏折叠已覆盖写作/协作切换；阅读不需要专门模式，想看直接看 |
| 质量可见性只留三层 | 砍第2层 | 段落级标注 v1 太重，文档树标记 + AI 聊天已覆盖发现→修复闭环 |
| 引擎节点详情保留审核结果 | 保留 | 监控工作流执行时不想跳页去工作室看 |
| 引擎节点详情不做上下文报告 | 砍 | 日志和 Prompt 回放已足够排查问题 |
| 分析实验室暂不动 | 保留 | 功能距离当前阶段较远，后续再细化 |
| 不做费用概览 | 砍 | 对接上游未计费，自己计费工作量大且无必要 |
| 双层设置（全局+项目） | 保留 | 参考 Claude/Codex Agent 的全局配置+项目配置模式，相辅相成 |
| 设置不独立成页 | 保留 | 侧边面板形式，不跳页 |

---

*最后更新: 2026-04-30*
