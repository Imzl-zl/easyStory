# easyStory UI 交互与视觉规格白皮书 (V5.0 - 终极版)

| 字段 | 内容 |
|---|---|
| 设计语言 | **水墨流 (Ink-Flow): 现代禅意与数字工笔** |
| 核心哲学 | **“以文为本，以简御繁”** |
| 视觉基调 | 极致留白、高对比度灰阶、湖蓝/电光青点缀、纸张物理质感 |

---

## 1. 设计哲学：水墨流的三个维度

### 1.1 负空间主权 (Sovereignty of Negative Space)
- **理念**：留白不是空白，而是“呼吸”。我们拒绝使用厚重的边框和背景色块，利用间距（Margin/Padding）的疏密来建立视觉秩序。
- **落地**：界面中 85% 以上的区域应保持纯净底色。模块切换通过明度微差（1% 灰阶变化）而非物理线条。

### 1.2 物理隐喻：纸、墨、砚 (Tactile Metaphor)
- **纸 (Paper)**：背景不是冰冷的像素，而是带有微弱噪点（Noise）和纤维感的宣纸纹理。
- **墨 (Ink)**：AI 生成的内容不应“弹出”，而应像墨水在宣纸上**晕开（Bleeding）**，文字从模糊到清晰。
- **砚 (Stone)**：功能面板（Surface）具有磨砂质感，触感坚实。

### 1.3 瞬时触达 (Instant Accessibility)
- **理念**：符合国人“快捷、聚合”的效率需求，但保持表面的极简。
- **落地**：引入 `Ctrl + K` 全局命令中心。90% 的功能（如新建、导出、切换 Skill）不占用视觉位，通过快捷指令唤醒。

---

## 2. 全局样式参数 (The Visual Code)

### 2.1 配色系统 (Color Tokens)
| Token | 浅色 (Fresh Paper) | 深色 (Midnight Ink) | 备注 |
| :--- | :--- | :--- | :--- |
| `--canvas` | `#FAFAFA` (米白) | `#09090B` (黑曜石) | 全局底色，带 2% Noise 纹理 |
| `--surface` | `#FFFFFF` (纯白) | `#18181B` (深灰) | 弹出层/主编辑面板 |
| `--ink` | `#171717` (炭黑) | `#E4E4E7` (柔和白) | 核心正文，宋体渲染 |
| `--mist` | `#737373` (烟灰) | `#71717A` (淡灰) | 辅助信息/占位符 |
| `--brand` | `#0891B2` (湖蓝) | `#22D3EE` (电光青) | 核心行动点/进度条 |
| `--danger` | `#EF4444` | `#F87171` | 节点报错/冲突提醒 |

### 2.2 字体排版 (Typography)
- **正文区 (Reading)**: `Source Han Serif SC` (思源宋体)，18px，1.85 行高，2em 首行缩进。
- **交互区 (UI)**: `PingFang SC` (苹方)，14px，字重 500。
- **代码/参数 (Mono)**: `JetBrains Mono`，12px。

---

## 3. 核心页面 ASCII 线框图与交互详述

### 3.1 门户页：灵感书架 (Project Portal)
*视觉重点：像实体书架一样陈列，突出“作品感”。*

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ easyStory  [ 🔍 Ctrl + K 检索一切... ]                 [+ 开启新卷] [🌙/☀️] │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  📖 书架 (Library)                                        [ 布局：封面 ▤ ] │
│                                                                          │
│  ┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐      │
│  │ 📘 长生路 (修仙)  │   │ 📗 赛博长安 (分析) │   │ 📔 快速开始       │      │
│  │                  │   │                  │   │ ──────────────── │      │
│  │ ─────────────── │   │ ─────────────── │   │ - 玄幻框架       │      │
│  │ 进度: 30%        │   │ 分析进度: 100%    │   │ - 都市框架       │      │
│  │ [===>          ] │   │ [===========>]   │   │                  │      │
│  │ ━━━━━━━━━━━━━━━━ │   │ ━━━━━━━━━━━━━━━━ │   │                  │      │
│  │    [执笔] [管理]  │   │    [报告] [模仿]  │   │    [套用模板]    │      │
│  └──────────────────┘   └──────────────────┘   └──────────────────┘      │
│                                                                          │
│  [ 🕒 最近修改：第三章 破晓 - 15分钟前 ]                    [ ⚙️ 全局配置 ]  │
└──────────────────────────────────────────────────────────────────────────┘
```
- **微操**：卡片封面支持自定义背景色。悬停时，卡片产生 `1.02` 倍微缩放，并显示该项目的 Token 消耗明细概览。

---

### 3.2 创作孵化器：对话式设定 (Creative Incubator)
*视觉重点：左感性，右理性。对话流与 Schema 画布的实时镜像。*

```text
┌────┬───────────────────────────────────────┬───────────────────────────────┐
│ ←  │ 🤖 助手：我们要创造一个什么样的世界？     │  📄 实时设定看板 (Live Schema) │
├────┼───────────────────────────────────────┼───────────────────────────────┤
│    │                                       │                               │
│    │  [AI]: 你想写个什么样的故事？           │  项目名称: [ 待识别 ]          │
│    │  [用户]: 我想写个修仙小说，主角林风。   │  类型: [ 修仙 ] ✨            │
│    │                                       │                               │
│    │  [AI]: 收到。[林风] 已作为主角。       │  ┌── 主角 (Protagonist) ──┐   │
│    │        他的核心能力是？               │  │ 姓名: 林风 (已同步)     │   │
│    │                                       │  │ 能力: [ 待填 ]          │   │
│    │  └─────────────────────────┘   │
├────┼───────────────────────────────────────┤                               │
│ ⚙️ │ [ 💬 描述你脑中的画面...      ] [发送] │      [ ✨ 固化设定并开启项目 ] │
└────┴───────────────────────────────────────┴───────────────────────────────┘
```
- **核心逻辑**：左侧每发送一条消息，右侧对应的 Schema 字段会有“墨水滴落”的扩散动画，显示数据已被捕获。

---

### 3.3 小说分析工作台 (Analysis Workspace)
*视觉重点：数据可视化。将晦涩的文风分析转化为直观的图表。*

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ← 返回  文风分析：[ 斗破苍穹.txt ]                                         │
├────────────────────────────┬─────────────────────────────────────────────┤
│  📁 章节采样 (采样率 15%)   │  📊 深度特征图谱 (Analysis)                  │
├────────────────────────────┼─────────────────────────────────────────────┤
│ [x] 智能采样策略            │  [ 文风雷达图 ]       [ 常用词云 ]           │
│     - 头部: 5章             │      (优雅)           (恐怖如斯)             │
│     - 尾部: 5章             │  (快节奏)---●         (斗气)                 │
│                            │      (华丽)           (异火)                 │
│ [ ] 手动勾选章节            │                                             │
│                            │  ┌── 模仿 Skill 建议 ─────────────────┐     │
│ 1. 第一章: 陨落... [x]      │  │ 核心提示词：多用短句，强调爆发感。       │     │
│ 2. 第二章: 斗气... [x]      │  └───────────────────────────────────────┘     │
├────────────────────────────┴─────────────────────────────────────────────┤
│ [ 进度: ████████████░░░ 80% ]   [ 正在分析：剧情结构模型... ]              │
└──────────────────────────────────────────────────────────────────────────┘
```
- **微操**：点击雷达图的顶点，可查看该特征对应的原文范例。

---

### 3.4 核心工作台：沉浸式编辑器 (Workspace)
*视觉重点：全功能工作台，三栏结构，但功能在写作时自动隐身。*

```text
┌───┬──────────────┬───────────────────────────────────────────┬──────────────┐
│   │ 📖 目录树     │  第五章：潜龙在渊              [Mode: 手动]│ ☁️ 灵感流 (AI)│
├───┼──────────────┼───────────────────────────────────────────┼──────────────┤
│ 📖│ 📁 设定仓库   │                                           │              │
│   │  ├ 人物关系   │         [ 🔗 参考：大纲 v2 | 前2章 ]        │ [?] 建议：   │
│ 📝│  └ 世界地图   │                                           │              │
│   │ ───────────  │    窗外大雪纷飞。                          │ 此处的冲突感  │
│ 👥│ 📄 第一章     │    林风盯着手中的断剑，眼神冰冷。          │ 建议增加更多  │
│   │ 📄 第二章     │    “还没有结束...”他低声说道。             │ 细节描写。    │
│ 🌍│ [🔄 第五章]   │                                           │              │
│   │ 📄 第六章     │    (宋体 / 1.85行高 / 21.6%黄金边距)       │ [ 采纳 ]      │
│   │              │                                           │ [ 忽略 ]      │
├───┼──────────────┤                                           │              │
│ ⚙️│ [快捷命令 /]  │ [ 2.4k 字 | 消耗: ￥0.12 | 🟢 实时同步 ]   │ [ 收起助手 ]  │
└───┴──────────────┴───────────────────────────────────────────┴──────────────┘
```
- **微操**：
  - **Zen Mode**：当用户开始连续输入超过 10 秒，左右侧边栏以 `cubic-bezier` 曲线平滑淡出，仅留编辑器，背景变为纯色。
  - **上下文胶囊**：点击顶部的“参考胶囊”，浮窗显示 AI 此时注入的上下文全文。

---

### 3.5 工作流编排器 (Workflow Builder)
*视觉重点：逻辑可视化。支持表单与 YAML 源码的 3D 翻转切换。*

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ ← 编排模式  项目: [ 长生路 ]                                    [保存] [执行] │
├────────────────────────────┬────────────────────────────────────────────────┤
│  视觉逻辑画布 (Canvas)      │  节点参数配置 (Property)           [表单] [YAML]│
├────────────────────────────┼────────────────────────────────────────────────┤
│  ┌───────────┐             │  节点 ID: node_writer_01                       │
│  │ 灵感提取  │             │  名称: 模仿金庸风格生成                        │
│  └─────┬─────┘             │                                                │
│        │                   │  关联 Skill: [ 射雕文风库 v3 ] [⚙️]             │
│  ┌─────▽─────┐             │                                                │
│  │ 章节生成  │●────────────┤  上下文加载:                                   │
│  └─────┬─────┘             │  [x] 注入大纲  [x] 注入前 3 章                │
│        │                   │                                                │
│  ┌─────▽─────┐             │  审核规则:                                     │
│  │ 逻辑校验  │             │  [x] 战力平衡  [ ] 敏感词                     │
│  └───────────┘             │                                                │
├────────────────────────────┴────────────────────────────────────────────────┤
│ [ + 新增节点 ]  [ 🔍 模拟节点运行 ]                         [ 🗑️ 彻底删除流程 ] │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.6 审核与精修：对比视图 (Review & Diff)
*视觉重点：极低认知负担的改动对比。*

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ← 返回  正在精修：[ 第五章 ]                                [一键采纳修改] │
├──────────────────────────────┬───────────────────────────────────────────┤
│  左：原稿 (Read-Only)         │  右：AI 建议 (可编辑)                      │
├──────────────────────────────┼───────────────────────────────────────────┤
│                              │                                           │
│  林风走进了房间。             │  林风[推门而入]，屋内一阵霉味扑面。        │
│                              │  (修改原因：环境描写缺失)  [✅采纳] [❌拒绝] │
│                              │                                           │
│  他感到非常生气。             │  他[紧握双拳，指节因用力而微白]。          │
│                              │  (修改原因：侧面描写替代)  [✅采纳] [❌拒绝] │
│                              │                                           │
├──────────────────────────────┴───────────────────────────────────────────┤
│ [ 💬 觉得不满意？告诉 AI：语气再冷酷一点...                     ] [重生成] │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 主题与交互微操 (Theme & Physics)

### 4.1 深度主题逻辑
- **白色模式 (Fresh Paper)**：不使用纯白底色，加入 1% 的灰和噪点，模拟宣纸。强调色使用深湖蓝。
- **黑色模式 (Midnight Ink)**：背景使用 `#09090B`，正文字体使用柔和的 `#E4E4E7`（防眩光）。强调色为电光青。
- **主题切换**：采用“墨迹渗透”过渡，从点击点开始，新主题色向四周圆形扩散覆盖，时长 0.6s。

### 4.2 针对国人习惯的“文学排版”
- **自动缩进**：开启该模式后，输入回车，下一行自动空出两个全角字符位。
- **排版灯**：编辑器右下角有一个极简的圆点，绿色代表“排版规范已开启”，红色代表“存在标点误用”。

---

## 5. 审核与落地自检 (Checklist)

1.  **极简度**：是否所有的边框宽度都保持在 0.5px 或 1px？（是）
2.  **审美**：是否优先使用了思源宋体作为文学内容的呈现？（是）
3.  **功能完整性**：14 个核心模块（包括采样、分析、节点系统、多级上下文）是否都有对应的 UI 位置？（是）
4.  **符合直觉**：返回在左上，下一步在右下，设置在左下。

---

*文档版本: 5.0.0 (Ultimate Full-Spec)*  
*更新时间: 2026-03-15*  
*设计师建议：在前端开发中，务必对所有的 Transition 使用 `cubic-bezier(0.4, 0, 0.2, 1)` 以确保系统的物理感一致。*
